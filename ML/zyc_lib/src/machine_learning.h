#ifndef _MACHINE_LEARNING_H
#define _MACHINE_LEARNING_H

#include <string>
#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <string>
#include <vector>
using namespace std;

#define YES 0
#define NO -1

#define NUMERICAL_RANGE 10000

static int idum = -time(NULL);
#define forn(i, n) for(int i = 0; i < n; i++)
const int MATRIX_SIZE = 300; // 300左右是极限了,否则ans什么都就要开成全局变量
const int MOD = 1000000007;

#endif
